# takes something, returns nothing
def add_with_args(a, b): # This function is defined to take two arguments a and b, and return nothing. It will add the two numbers together and print the result.
    c = a + b # This line adds the two numbers a and b together and stores the result in the variable c.
    print("Sum is", c) # This line prints the message "Sum is" followed by the value of c, which is the sum of a and b.


add_with_args(10, 20) # This line calls the function add_with_args with the arguments 10 and 20. The function will execute the code inside it, add the two numbers together, and print the result, which will be "Sum is 30".