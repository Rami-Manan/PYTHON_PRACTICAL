a = lambda a,b: a+b # lambda is a anonymous function which can take any number of arguments but can only have one expression. It is used to create small, throwaway functions without the need for a formal function definition.
print(a(4,3)) # this will print 7 because the lambda function takes two arguments a and b, adds them together and returns the result.
