l1 = [50,20,80,10,60,40] # Create a list l1 with the elements 50, 20, 80, 10, 60, and 40
 
 # Initialize a variable i to 0, which will be used as an index to access elements in the list l1

i=0 # Start a while loop that will run as long as i is less than or equal to 5, which is the index of the last element in the list l1
while i<=5: # Inside the loop, print the element at index i of the list l1, followed by a space instead of a newline character
    print(l1[i], end=' ') # Increment i by 1 at the end of each iteration of the loop to move to the next index in the list l1
    i+=1 # Increment i by 1 at the end of each iteration of the loop to move to the next index in the list l1